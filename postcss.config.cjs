module.exports = {
  plugins: {
    // O plugin do Tailwind CSS processa as diretivas do Tailwind (como @tailwind) 
    // e as classes de utilitário (como 'text-center') no seu CSS.
    tailwindcss: {},

    // O Autoprefixer adiciona os prefixos de fornecedores (-webkit-, -moz-, etc.) 
    // às regras de CSS para garantir a compatibilidade com navegadores mais antigos.
    autoprefixer: {},
  },
}
