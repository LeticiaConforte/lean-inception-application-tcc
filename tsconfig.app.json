{
  "compilerOptions": {
    // Permite a interoperabilidade entre módulos CommonJS e ES Modules.
    "esModuleInterop": true,
    // Pula a verificação de tipos em arquivos de declaração (.d.ts) de bibliotecas, o que pode acelerar a compilação.
    "skipLibCheck": true,
    // Define a versão do JavaScript que o TypeScript irá gerar. ES2022 é uma versão moderna.
    "target": "ES2022",
    // Permite que arquivos JavaScript (.js) sejam incluídos na compilação.
    "allowJs": true,
    // Permite a importação de arquivos .json como módulos.
    "resolveJsonModule": true,
    // Garante que cada arquivo seja tratado como um módulo, mesmo que não tenha imports/exports.
    "moduleDetection": "force",
    // Garante que cada arquivo possa ser transpilado de forma isolada, sem depender de outros arquivos.
    "isolatedModules": true,
    // Habilita todas as checagens de tipo estritas do TypeScript (recomendado).
    "strict": true,
    // Reporta erro para variáveis locais não utilizadas.
    "noUnusedLocals": true,
    // Reporta erro para parâmetros de função não utilizados.
    "noUnusedParameters": true,
    // Reporta erro se um 'case' em um 'switch' não tiver 'break' ou 'return'.
    "noFallthroughCasesInSwitch": true,
    // Configura a forma como o JSX (usado no React) é compilado.
    "jsx": "react-jsx",
    // Define as bibliotecas de declaração de tipos que o TypeScript deve incluir na compilação.
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    // Define o sistema de módulos a ser usado. 'ESNext' usa a versão mais recente dos módulos ES.
    "module": "ESNext",
    // Estratégia de resolução de módulos. 'bundler' é a recomendada para aplicações modernas com bundlers como o Vite.
    "moduleResolution": "bundler",
    // Define a pasta base para a resolução de caminhos (paths).
    "baseUrl": ".",
    // Cria aliases para caminhos de importação. Aqui, '@/*' aponta para './src/*'.
    "paths": {
      "@/*": ["./src/*"]
    },
    // Habilita a compilação de projetos referenciados.
    "composite": true,
    // Arquivo para armazenar informações sobre a última compilação, para builds incrementais.
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    // Inclui tipos específicos para o ambiente do Vite.
    "types": ["vite/client"]
  },
  // Especifica os arquivos e pastas que devem ser incluídos na compilação.
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx"
  ],
  // Especifica os arquivos e pastas que devem ser excluídos da compilação.
  "exclude": [
    "node_modules",
    "dist"
  ]
}